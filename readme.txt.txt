Step1 :
      Build The spring-boot-ems maven project
    
      It will create spring-boot-ems-0.0.1-SNAPSHOT.jar file

Step2 :
      Go Inside Angular project and build angular project
      
      Install Node JS 

      npm i -g @angular/cli 

      ng serve --prod

      It will create a dist/angular folder which is production ready code

Step 3:

     Type below command from direcotry containing docker-compose.yml
     
     docker-compose up -d
     

  
          
    




